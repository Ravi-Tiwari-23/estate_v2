import Link from 'next/link';
import { ArrowLeft, Construction } from 'lucide-react';
export default async function Placeholder({params}){const {slug}=await params;const title=slug.map(s=>s.replaceAll('-',' ')).join(' / ');return <main className="flex min-h-screen items-center justify-center bg-cream p-6"><div className="max-w-xl rounded-[30px] border border-ink/10 bg-white p-10 text-center shadow-soft"><Construction className="mx-auto text-green" size={34}/><p className="eyebrow mt-6">ROUTE READY</p><h1 className="mt-3 font-serif text-4xl capitalize">{title}</h1><p className="mt-4 leading-7 text-ink/55">This destination is wired into the homepage and ready for its dedicated product experience.</p><Link className="btn-dark mt-7" href="/"><ArrowLeft size={16}/> Back to home</Link></div></main>}
